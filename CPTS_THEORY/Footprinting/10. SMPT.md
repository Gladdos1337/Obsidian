default port 25
newer 587 -> used to receive mail from aujthenticated users/servers, usually using STARTTLS command to switch the existing plaintext con to an encrpyted con.

**The authjentication data is protected and no longer visible in plaintext over the network.**

SMPT works uncencrpyted! withgout further measures and tgransmits all commands, data or authentication information in plain text. To prevent unauthrized reading of data, the SMTP is used to conjunction with **SSL/TLS encrpytion**. Under certain circumstances, a server uses a port other than the standard 25for encrpyted connection, for example tcp port 465.

![[Pasted image 20260610220804.png]]

SMTP has two disadvantages interent to the network protocol.

1. sending email using SMPT does not return a usable delivery confirmation
2. users are not authenticated when a connection is established, and the sender of an email is therefore uinreliable. as an result, open SMTP relays are often missused spam en masse. the orginators use arbitrary fake sender addresses to purpose to not be traced (mail spoofing). Today, many different security techniques are used to prevent the misuse of SMTP servers. eg sus mails are rejected or moved to quarantine (spam). 

because of this, an extension for SMPT has been deeloped called EXtended SMTP ESMTP.  When people talk about SMPT in general,

To inertact with SMTP server we can use telent to init TCP connection with SMTP server. An actual init of the session is done with command **HELO** / **EHLO**

telnet 10.129.14.128 25


*HELO mail1.inlanefreigth.htb*

*EHLO mail1*

Command VRFY can be used to enumerate existing users.
However it doesnt always work. Depending on how the SMTP server is configured, the SMTP server may issue code 252 and confirm the existance of a user that does not exist on the system.

https://serversmtp.com/smtp-error

sometimes we may have to work through a web proxy..
we can also make this web proxy connect to the SMTP server. The command that we would send would look like:

CONNECT 10.129.14.128:25 HTTP/1.0


