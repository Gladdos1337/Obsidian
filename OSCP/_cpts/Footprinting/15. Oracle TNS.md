`Oracle Transparent Network Substrate` (`TNS`)

> server communicatin protocol
> supports IPX/SPX and TCP/IP protocol stacks
> used in large, complex databases
> has built in encrpytion
> updated to ipv6 and ssl/tls encrpytion, used for :
> name resoultion, connection management,  load balacing, security


default port : 1521
can be remotely maanged in oracle 8i/9i but not 10g/11g

 default config has some security features, such as basic auth using combiantion of hostnames, ip addresses, usernames and passwords.

The configuration files for Oracle TNS are called `tnsnames.ora` and `listener.ora` and are typically located in the `$ORACLE_HOME/network/admin` directory. The plain text file contains configuration information for Oracle database instances and other network services that use the TNS protocol.

oracle tns is often used with other oracle services

Oracle 9 default password : CHANGE_ON_INSTALL
Oracle 10 has no default password
Oracle DBSNMP default password :dbsnmp
Another example would be that many organizations still use the `finger` service together with Oracle, which can put Oracle's service at risk and make it vulnerable when we have the required knowledge of a home directory.

#### Tnsnames.ora
contains necessery info for clients to connect to service

#### Listener.ora
is a server-side configuration file that defines the listener process's properties and parameters, which is responsible for receiving incoming client requests and forwarding them to the appropriate Oracle database instance.

Oracle databases can be protected by using so-called PL/SQL Exclusion List (`PlsqlExclusionList`). It is a user-created text file that needs to be placed in the `$ORACLE_HOME/sqldeveloper` directory, and it contains the names of PL/SQL packages or types that should be excluded from execution. Once the PL/SQL Exclusion List file is created, it can be loaded into the database instance. It serves as a blacklist that cannot be accessed through the Oracle Application Server.



tools used for enumeration

ODAT - oracle db attacking tool 

```
sudo nmap -p1521 -sV 10.129.204.235 --open
```

nmap bruteforce:

```
sudo nmap -p1521 -sV 10.129.204.235 --open --script oracle-sid-brute
```
