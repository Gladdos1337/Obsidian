# Port 110/143 - POP3 / IMAP (Dovecot)

Both services were noted but access was achieved via **SquirrelMail** webmail on port 80 rather than directly.

## SquirrelMail

- URL: `http://10.113.130.138/squirrelmail/`
- Login: `milesdyson` / `cyborg007haloterminator`
- Emails found here revealed credentials / context

## CVE Note

**CVE-2025-30090** — XSS via email headers in SquirrelMail ≤1.4.23 and 1.5.x (not required to complete the box)
